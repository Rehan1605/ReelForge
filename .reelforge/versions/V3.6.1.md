# ReelForge V3.6.1 — Storage Lifecycle Audit & Operations Specification

> **Development Date**: 2026-09-14
> **Commit**: `d500583` (*feat(v3): cloud portability, durable jobs, and worker workspace isolation*)
> **Status**: Verified & Committed
> **Primary Tests**: `scratch/test_v36_layer1_storage_audit.py` (19/19 passing)
> **Documentation**: `WORKER_OPERATIONS.md`, `WORKER.env.example`

---

## 1. What Changed
- **Storage Lifecycle Audit**:
  - Audited all file read and write operations across the entire codebase to formalize the lifecycle of persistent vs ephemeral artifacts.
  - Verified that Brain Objects (`brains/*.json`) and MongoDB documents remain untouched by ephemeral cleanups.
  - Verified that video MP4, thumbnails, and `*.info.json` files are strictly treated as ephemeral media scratch deleted on completion (`KEEP_VIDEOS=False`).
  - Audited vision temporary frame directories (`reelforge_vision_*`) under system temp, verifying cleanup in both success and failure paths.
- **Operations & Runtime Contract**:
  - Authored `WORKER_OPERATIONS.md`: comprehensive 350+ line operational specification covering environment variables, MongoDB authoritative rules, network dependencies, health pre-flight assertions, and failure recovery.
  - Created `WORKER.env.example` with full configuration templates (zero real secrets).
- **Test Suite Verification**:
  - Implemented `scratch/test_v36_layer1_storage_audit.py` containing 19 lifecycle tests.

## 2. Tests & Outcomes
- Ran `python scratch/test_v36_layer1_storage_audit.py`: 19/19 tests passed (0 failures, 0 errors).

## 3. Git Status
- Committed to branch `main` in checkpoint commit `d500583`.
